1- Please set the working directory to the current folder.

2 - If SDMTools package is not installed already then you need to install it manually. 
To do so just uncomment the installation line from the code in the 1st chunk and install the package.

3 - you might need to change the dataset path on line 295, kindly check before running the file.